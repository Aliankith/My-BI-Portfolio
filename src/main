import React from 'react';
import ReactDOM from 'react-dom/client';
import App from './App.jsx';
// Tailwind CSS is typically managed here, but Vercel/Tailwind will handle it via class names
// in your App.jsx, so no need for a separate CSS file for this portfolio.

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
);
